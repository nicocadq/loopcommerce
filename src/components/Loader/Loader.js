import React from "react";

import { Wrapper, StyledLoader } from "./Loader.styles";

const Loader = () => {
  return (
    <Wrapper>
      <StyledLoader />
    </Wrapper>
  );
};

export default Loader;
